# Build Week scope

## New competition work

The ColonyMind application, product experience, organism-network abstraction,
colony-of-organisms layer, resource-aware comparison, GPT-5.6 curriculum and
explanation features, evidence reports, tests, documentation, and deployment
under `openaidev.automationfreelancer.com` are Build Week deliverables.

## Pre-existing references

Earlier experiments under `D:/GitHub/AI` contain research prototypes for Ideal
Free Distribution, unlabeled geometric learning, dynamic neural graphs,
organisms, colonies, ecosystems, lifecycle accounting, deterministic reports,
and visual simulation.

They may be inspected and selectively adapted, but every reused component must
be listed here with:

- original path and version;
- whether it was copied, rewritten, or used only as conceptual reference;
- the new ColonyMind functionality added during Build Week;
- tests that demonstrate the new behavior.

Do not describe pre-existing work as newly created. Judges should be able to
distinguish the earlier experimental foundation from the competition product.

## Reuse manifest

No implementation component has been imported yet. Add entries as reuse occurs.
